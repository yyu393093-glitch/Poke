# 任务 04：戳一戳与多端交互

## 开发目的
实现「戳一戳」催进度（自动生成消息 + 灯仔飞行动画 + 地图公开记录），以及「多端交互」的模拟——戳一戳的消息要「飞」进一个假飞书窗口，让对方在"飞书"里收到。对应分镜 0:40–0:56。

## 前置依赖
- 任务 02（`/api/poke` 接口）
- 任务 03（网络图渲染，能点击节点）

## 需要完成的事项

### 1. 戳一戳按钮 + 自动消息 — `src/components/PokeAction.jsx`

- 点击某个节点（上游）后，在 NodeCard 里出现「戳一戳」按钮
- 点击「戳一戳」→ 调 `POST /api/poke { from: currentUser, to: nodeId }`
- 拿到返回的 `{ message, channel }`，消息展示为一条自动生成的文本气泡
- 用户**无需自己编辑**文案（凸显「自动生成」）

### 2. 灯仔飞行动画
- 点击戳一戳后，一个「灯仔」小图标从当前节点出发，沿贝塞尔曲线飞向**假飞书窗口**方向
- 用 CSS transition / `requestAnimationFrame` 实现，飞行约 1.5s，带拖尾光点
- 飞行结束后灯仔消失（或定格在假飞书窗口上方）

### 3. 公开记录 — `src/components/PokeLog.jsx`
- 网络图角落显示「戳一戳」公开记录列表，每条一个 `👍 小陈 → 陈总 17:57:32`
- 每次戳一戳后新增一条（`ADD_POKE` 写入全局状态）
- 时间戳（如 `17:57:32`）用本地计数器生成：从 `17:57:00` 起每秒 +1（与任务 06 时钟同源），不读系统时间，保证 Mac/Windows 一致

### 4. 假 IM 窗口 — `src/components/FakeIMWindow.jsx`

**目标：模拟「多端交互」，戳一戳消息"飞进"假飞书，对方在"飞书"里收到（H2）。**

- 在渲染页下半部放一个迷你聊天窗口，标题栏写 `飞书 · 陈总`（带飞书风 logo，可用文字/emoji 代替，**不用真实飞书 logo**）
- 窗口内是消息气泡列表：左侧对方头像 + 右侧/左侧消息气泡
- 收到 `channel: 'feishu'` 的消息时，向该窗口 `push` 一条消息气泡（内容就是 `/api/poke` 返回的 message）
- 消息气泡旁显示一个「已读」红点，1 秒后消失（模拟对方已读）
- 「已读」后，在对方侧再 `push` 一条回复气泡，内容为 `/api/poke` 返回的 `reply`（如「收到，10 分钟内发你 🙌」），模拟对方回复
- 窗口内可预置 1~2 条历史消息，让窗口看起来真实

### 5. 多端布局
- 渲染页左下/右下放三个假 IM 图标：飞书、企业微信、钉钉（文字标签即可，**不用真实 logo**）
- demo 主流程只弹「假飞书」窗口；企微、钉钉作为静态图标收在角落（收尾演出时露脸）

## 约束
- **不要**用飞书/企微/钉钉的真实 logo 或品牌图片（H1/H2），用文字或 emoji 占位
- 戳一戳消息**自动生成**，不要做让用户编辑消息的输入框
- 灯仔飞行动画最多 1.5s，不要过度复杂
- 假 IM 窗口是纯前端组件，不真实发消息

## 验证方法
1. 点击「品牌素材」节点 → NodeCard 出现「戳一戳」按钮
2. 点击「戳一戳」→ 出现自动生成的消息「陈总好，首页设计稿还差品牌素材…」
3. 灯仔从「首页设计稿」飞向假飞书窗口，约 1.5s
4. 假飞书窗口新增一条对应消息气泡，带「已读」红点后消失
5. 网络图角落 PokeLog 新增一条 `👍 小陈 → 陈总`
6. 企微、钉钉图标在角落可见但不弹窗
7. 控制台无报错
