# 任务总览：「戳戳」(Poke) MVP Demo

## 项目简介
- **项目名：** 戳戳 Poke（桌面悬浮协作依赖网络图）
- **演示主题：** 下班前 180 秒 · Finish & Align
- **一句话：** 一个本地运行的网页 Demo——桌宠「灯仔」作为入口，点击跳转到协作依赖网络图，演示「谁卡着谁 → 戳一戳催进度 → 自动通知下游 → 涟漪影响 → 下班关灯」的完整闭环。

## 硬约束（所有任务都必须遵守）
1. **H1 飞书 = 模拟**：不接飞书/企微/钉钉的真实 OAuth、真实 API、真实数据。
2. **H2 多端 = 模拟**：只有渲染页是真的；飞书/企微/钉钉全是假窗口。
3. **H3 只本地部署**：一条命令本地跑，不部署任何云。
4. **桌宠最简版**：桌宠只做入口，点击跳转 `/network`，不承担网络图功能。

## 技术栈
- 前端：React + Vite + Tailwind CSS + React Router
- 后端：Node.js + Express（本地 mock，扮演「飞书服务器」）
- 数据：`server/mock-data.json`（写死的假飞书数据）

## 任务拆解与依赖关系

```
01 项目骨架与桌宠（无依赖）
├──▶ 02 本地 mock 后端 / 飞书连接模拟（依赖 01）
├──▶ 03 协作网络图（依赖 01 + 02）
│
04 戳一戳与多端交互（依赖 02 + 03）
│
05 状态通知涟漪指标（依赖 02 + 03 + 04）
│
06 关灯收尾与全流程联调（依赖全部）
```

## 全局共享约定

以下约定在所有任务中生效，每个任务文档不再重复。

### 页面路由（两个页面）
```javascript
// 入口页：桌宠
'/'          → DeskPet（桌宠灯仔，点击跳转）

// 渲染页：协作网络图 + 全部演出
'/network'   → NetworkGraph + 戳一戳 + 通知 + 涟漪 + 关灯 + 假 IM 窗口 + 收尾
```

### 数据模型（见 Poke_SPEC.md 第 5 节，字段名统一）

```typescript
interface Node {
  id: string; name: string; owner: string; dept: string;
  status: 'done' | 'doing' | 'todo';
  isBottleneck: boolean; isDelayed: boolean;
  x: number; y: number;
}
interface Edge { id: string; from: string; to: string; isCritical: boolean; }
interface Poke { id: string; from: string; to: string; message: string; channel: string; time: string; }
interface Notification { id: string; to: string; type: 'poke'|'upstream_done'|'offline_boundary'; message: string; channel: string; }
interface Ripple { downstreamTasks: string[]; departments: string[]; reachUsers: number; }
interface DailyMetrics { doneToday: number; alignedPeople: number; blocked: number; }
```

### 预设 demo 网络（写死，勿改 id）

| id | name | owner | dept | status | isBottleneck | isDelayed |
|---|---|---|---|---|---|---|
| n_req | 需求文档 | 王姐 | 产品部 | done | false | false |
| n_brand | 品牌素材 | 陈总 | 设计部 | doing | false | true |
| n_design | 首页设计稿 | 小陈 | 设计部 | doing | true | false |
| n_dev | 前端开发 | 老李 | 研发部 | todo | false | false |
| n_test | 联调测试 | 小赵 | 研发部 | todo | false | false |
| n_copy | 运营文案 | 阿May | 运营部 | done | false | false |

连线（关键路径：req→brand→design→dev→test；并行：req→copy）：
```
req → brand → design → dev → test    （isCritical: true）
req → copy                            （并行分支）
design → dev, design → test           （design 阻塞两个下游）
```

### 本地 Mock API（8 个，见 SPEC 第 6 节）

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/api/feishu/auth` | 假授权，返回假 token |
| GET | `/api/feishu/data` | 返回 mock 任务数据 |
| POST | `/api/ai/parse` | 本地规则「解析」出节点+依赖 |
| POST | `/api/ai/approve` | 审批通过，生成正式网络 |
| POST | `/api/poke` | 生成戳一戳消息 + 指定 channel |
| POST | `/api/node/complete` | 标记完成 + 生成下游通知 |
| POST | `/api/clock/off` | 关灯 |
| GET | `/api/metrics` | 今日对齐度指标 |

### 状态机

```
节点状态：  todo ──▶ doing ──▶ done
演示阶段：  IDLE(17:57) ─▶ OPEN(网络展开) ─▶ ACTIVE(戳/完成/涟漪) ─▶ OFF(18:00)
```

### 文件结构约定
```
Poke/
├── src/                    # 前端源码
│   ├── pages/
│   │   ├── DeskPetPage.jsx     # 入口页（桌宠）
│   │   └── NetworkPage.jsx     # 渲染页（网络图 + 全部演出）
│   ├── components/
│   │   ├── DeskPet.jsx         # 桌宠（最简）
│   │   ├── NetworkGraph.jsx    # 网络图
│   │   ├── NodeCard.jsx        # 节点详情
│   │   ├── PokeAction.jsx      # 戳一戳
│   │   ├── PokeLog.jsx         # 公开记录
│   │   ├── FakeIMWindow.jsx    # 假 IM 窗口
│   │   ├── RippleView.jsx      # 涟漪
│   │   ├── MetricsBar.jsx      # 指标
│   │   ├── ClockBar.jsx        # 时钟
│   │   └── EndingScreen.jsx    # 收尾演出
│   ├── context/GameContext.jsx # 全局状态
│   ├── api/gameApi.js          # API 封装
│   ├── App.jsx                 # 路由
│   └── main.jsx
├── server/
│   ├── index.js                # Express + 路由
│   ├── mock-data.json          # 假飞书数据 + 网络
│   └── package.json
└── Poke_SPEC.md                # 实现规格（本文档的上游）
```
