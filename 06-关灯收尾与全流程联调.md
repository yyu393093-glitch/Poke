# 任务 06：关灯、收尾演出与全流程联调

## 开发目的
实现「下班关灯」、收尾演出（标语 + 数据回放 + 三平台图标），并完成 180 秒全流程联调 + 边界兜底，确保 demo 可稳定复现。对应分镜 1:44–3:00。

## 前置依赖
- 任务 01–05 全部完成

## 需要完成的事项

### 1. 时钟 — `src/components/ClockBar.jsx`
- 渲染页右上角显示时钟，从 **17:57:00** 开始走秒
- 用 `setInterval` 每秒 +1，组件卸载时清理
- 演示约 3 分钟后走到 18:00（demo 用加速或不加速均可，建议真实走秒）

### 2. 收工 + 关灯 — `OffClock`（并入渲染页或独立组件）

- 渲染页放一个「收工」按钮（分镜 1:44–1:50 用）
- 点击 → 调 `POST /api/clock/off`
- 演出：小陈的节点（n_design）亮度降到 40%，切换到暖橙色「夜灯」状态
- 页面整体色调轻微变暗（关灯氛围）
- 时钟停在 18:00:00

### 3. 收尾演出 — `src/components/EndingScreen.jsx`

关灯后切换到收尾屏，按顺序播放（对应分镜 2:02–3:00）：

| 时间 | 内容 |
|---|---|
| 2:02–2:06 | 黑屏转场 |
| 2:06–2:10 | 核心标语逐字打出（打字机效果）：`下班前 3 分钟，把一天对齐。` |
| 2:10–2:16 | 副标语：`谁卡着谁，一眼看清；催进度，戳一下就好。` |
| 2:16–2:22 | 产品名 `戳戳 Poke` + 三平台图标（飞书/企微/钉钉，文字/emoji 占位，**不用真实 logo**） |
| 2:22–2:30 | 数据回放：`今日对齐 5 人 · 减少群聊 @ 40% · 阻塞时长 -35%` |
| 2:30–2:40 | 收口一句：`当依赖可见、催进度无压力，协作会不会更体面一点？` |
| 2:40–3:00 | 「重新开始」按钮（点击 `RESET` 回到入口页 `/`）+ 团队信息 |

### 4. 全流程联调测试

按 180 秒分镜完整走一遍，记录并修复问题：

```
入口页 `/`：桌宠浮现 → 点击桌宠跳转 `/network`
渲染页：飞书授权+解析隐喻（网络生长）→ 节点点亮 → 瓶颈红框 → 关键路径高亮
  → 延期 ⏰ → 通知条「阻塞 2 个下游」→ 节点详情
戳一戳：点「品牌素材」→ 自动消息 → 灯仔飞向假飞书 → 假飞书收到 → 公开记录
标记完成：n_design 黄→绿 → 自动通知下游 → n_dev/n_test 灰→黄点亮
指标：顶部「今日完成 3 项 · 对齐 5 人 · 阻塞 0」
涟漪：波纹扩散 → 影响面板（120 万用户）
收工：点击收工 → 关灯 → 时钟 18:00 → 收尾演出（标语/数据回放/平台图标）→ 重新开始
重置：重新开始回到入口页，状态归零，能再走一遍
```

### 5. 边界兜底（借鉴 GAME_DESIGN 降级方案）

- **后端没起（只开前端）**：`gameApi.js` 的 `request` 对失败做兜底——用内置的一份 mock 数据渲染网络图，demo 不崩
- **接口响应慢**：网络生长动画本身就慢，观众看不出卡
- **动画崩**：核心台词/文案是陈述句，靠文字也能把功能讲明白（见 DEMO_DESIGN 第 10 节）
- **快速连点「戳一戳」/「标记完成」**：加 loading 锁，不发送重复请求

## 约束
- 不要添加新功能，只做关灯、收尾、联调、兜底
- 三平台图标用文字/emoji 占位，**不用真实品牌 logo**（H1/H2）
- 收尾演出按第 3 步的时间顺序播放，不要跳步骤
- 时钟组件卸载时务必清理 `setInterval`

## 验证方法
1. 从入口页到收尾屏，180 秒分镜无 console 报错走完
2. 点击「收工」后节点关灯变暖橙、页面变暗、时钟停 18:00
3. 收尾屏标语逐字打出，随后数据回放 + 三平台图标依次出现
4. 点「重新开始」回到入口页，再点桌宠能重走一遍，状态归零
5. 停掉后端只开前端，打开 `/network` 仍能渲染网络图（走兜底），不白屏
6. 快速连点「标记完成」只触发一次请求
