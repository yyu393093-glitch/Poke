# 「戳戳」三人分工与协作

> **一句话总原则**：按「功能域」切三条主线，让三个人的**文件完全不重叠**；用 [00-开发契约.md](./00-开发契约.md) 把共同依赖的东西提前钉死，各自并行，最后小步合流。
>
> **先读**：[00-总览与依赖关系.md](./00-总览与依赖关系.md) 和 [00-开发契约.md](./00-开发契约.md)，再看本文。

---

## 1. 三条主线分工表

| 人 | 主线 | 负责 task | 只碰这些文件 | 绝不碰 |
|---|---|---|---|---|
| **A · 后端** | 数据与接口 | 02 | `server/` 整个目录 + `server/mock-data.json` | 所有 `src/` |
| **B · 网络图** | 网络图 + 状态流转 | 03 + 05 | `NetworkGraph.jsx`、`NodeCard.jsx`、`NetworkPage.jsx`、`RippleView.jsx`、`MetricsBar.jsx` | `server/`、PokeAction、FakeIMWindow、ClockBar、EndingScreen |
| **C · 交互演出** | 戳一戳 + 多端 + 收尾 | 04 + 06(第1~3步) | `PokeAction.jsx`、`PokeLog.jsx`、`FakeIMWindow.jsx`、`FlyingLamp.jsx`、`ClockBar.jsx`、`EndingScreen.jsx` | `server/`、NetworkGraph、NodeCard、NetworkPage、RippleView、MetricsBar |

**说明：**
- B 的 task 05 里「自动通知下游」要飞灯仔 → 复用 C 的 `FlyingLamp` 组件（契约第 7 节），**B 不自己写灯仔**。
- task 06 拆成两半：第 1~3 步（时钟、关灯、收尾演出）归 C；第 4~5 步（全流程联调、边界兜底）是**阶段 3 合流**的事，见第 5 节。

---

## 2. 阶段划分（6 小时时间线）

| 阶段 | 时间 | 谁 | 干什么 |
|---|---|---|---|
| 0 · 定契约 | 0:00–0:30 | 三人一起 | 各读一遍 00-总览 + 00-契约；建分支；确认 FlyingLamp 约定 |
| 1 · 地基 | 0:30–1:00 | 一个人（A 或最快者） | 做 task 01（骨架+路由+桌宠+全局状态+API 封装）；另外两人精读自己的 task |
| 2 · 三路并行 | 1:00–4:30 | 三人各干各 | A→02、B→03+05、C→04+06(1~3)，各自用假数据联调 |
| 3 · 合流联调 | 4:30–5:30 | 一人牵头+两人配合 | task 06 第 4 步：走通 180 秒分镜 |
| 4 · 打磨彩排 | 5:30–6:00 | 三人 | 修 bug、演示彩排 |

---

## 3. 依赖解耦三招（并行不互相等的关键）

### 招 1：接口契约先行
A 动手写后端**之前**，先把接口签名（请求/响应 JSON）发到群里——其实契约文档第 3 节已经写好了，A 只需确认「就按这个来」。B、C 不需要等 A 的接口跑通。

### 招 2：前端假数据占位
`gameApi.js` 的 `request()` 里先做一层兜底：**后端没起或接口没 ready 时，返回写死的假数据**。B、C 先用假数据把自己的界面写完，等 A 接口好了再把兜底换掉。
> 这正好是 task 06 第 5 步要求的「边界兜底」，提前做掉，一举两得。

### 招 3：可复用组件解耦
C 写完 `FlyingLamp`（灯仔飞行）后，在群里发一句「FlyingLamp 已就绪，props 见契约 7.1」。B 的「通知下游」直接 `import` 复用，不用等 C 的其他功能。

---

## 4. Git 协作

```
main
├── feat/backend   (A)
├── feat/network   (B)
└── feat/interact  (C)
```

- **每个 task 写完立刻 merge 回 main**，不要攒到最后。
- 会冲突的只有三个共享文件（契约第 6 节：`App.jsx` / `GameContext.jsx` / `gameApi.js`），阶段 1 定好后，谁要动先群里喊。
- merge 前 `git pull origin main`，冲突就地解，不要隔夜。

---

## 5. 合流联调分工（阶段 3）

- **牵头人：A**（懂接口全貌，适合走主流程）。
- A 按 [task 06 第 4 步](06-关灯收尾与全流程联调.md) 的清单走一遍 180 秒分镜，遇到卡点喊对应的人：
  - 后端数据不对 → A 自己修
  - 网络图/状态/涟漪不对 → B 修
  - 戳一戳/假 IM/收尾不对 → C 修
- 联调走通后，三人一起过 task 06 的「验证方法」，逐条打勾。

---

## 6. 避坑清单（vibe coding 三人并行最容易翻车的 5 个坑）

| # | 坑 | 解法 |
|---|---|---|
| 1 | AI 自作主张加功能越界（B 的 AI 顺手做了假 IM 窗口） | 喂 AI 时强调「只做本 task，约束里写了的都不做」 |
| 2 | 字段名/枚举不一致 | 契约第 1/4 节已锁死，喂 AI 时**把契约一起喂** |
| 3 | 改共享文件不打招呼 | 只认契约第 6 节的三张表，越界先喊 |
| 4 | 真实 logo 混进来 | 契约禁令第 1 条，用文字/emoji 占位 |
| 5 | 最后才合并 → 大爆炸 | 每个 task 完成就 merge，见第 4 节 |

---

## 7. 开工前 Checklist（三个人各自打勾）

- [ ] 读完 00-总览 + 00-契约 + 本文
- [ ] 拉好分支：`feat/backend` / `feat/network` / `feat/interact`
- [ ] 确认自己的文件清单（第 1 节表），划清不碰的边界
- [ ] A 在群里发一句「接口按契约第 3 节，现在开工」
- [ ] C 确认 FlyingLamp 的 props（契约 7.1）后，B 知道复用方式
- [ ] 每个人喂 AI 时：**task 文档 + 00-开发契约.md 一起喂**

---

*本文 v1.0 | 配套：[00-开发契约.md](./00-开发契约.md) · [00-总览与依赖关系.md](./00-总览与依赖关系.md) | 分工有变动先改这张表*
