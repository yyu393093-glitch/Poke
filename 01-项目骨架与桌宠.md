# 任务 01：项目骨架与桌宠

## 开发目的
搭建「戳戳」的前后端项目骨架，建立两个页面路由（入口页 `/` 和渲染页 `/network`），实现「桌宠」最简版——一个固定右下角的小角色，点击跳转到渲染页。本任务不实现网络图、不实现任何业务演出，只搭地基。

## 前置依赖
- 无

## 需要完成的事项

### 1. 前端项目骨架
- 用 Vite + React 创建项目（若已有 hackaphon 根项目则在其 `src/` 内新增，与旧 H.O.R.S.E 代码隔离）
- 安装并配置 Tailwind CSS（`@tailwindcss/vite` 插件）
- 安装 React Router（`react-router-dom`）

### 2. 页面路由 — `src/App.jsx`
- 建立两个路由：
  - `/` → `DeskPetPage`（入口页，桌宠）
  - `/network` → `NetworkPage`（渲染页，先用占位组件 `return <div>网络图 TODO</div>`）
- 用 `BrowserRouter` 包裹

### 3. 桌宠组件 — `src/pages/DeskPetPage.jsx` + `src/components/DeskPet.jsx`

**DeskPet（最简版，只做入口）：**
- 一个固定在屏幕右下角的圆形小角色（灯仔），`position: fixed; bottom: 24px; right: 24px`
- 视觉：暖黄色圆形 + 灯泡/夜灯 icon（可用 emoji 💡 或简单 SVG），带一点光晕
- 可选极简动画：轻微上下漂浮（`@keyframes float`，2s 循环）或眨眼，**不要做复杂动画**
- 点击整个桌宠 → `navigate('/network')`

**DeskPetPage：**
- 深色渐变背景（仿桌面壁纸）
- 底部一句小字提示：「17:57 · 下班前，点一下灯仔对齐今天的工作」（可选）

### 4. 后端骨架 — `server/`
- 创建 `server/package.json`（type 为 module），安装 `express`、`cors`
- 创建 `server/index.js`：Express 服务器，监听 **3001** 端口，启用 CORS + JSON 解析
- 创建一个占位端点：`GET /api/health` 返回 `{ ok: true }`
- 创建 `server/mock-data.json`（空占位 `{}`，后续任务 02 填充）

### 5. API 通信封装 — `src/api/gameApi.js`
- 导出统一的 `request(path, options)` 函数
- 请求前缀 `${VITE_API_URL || 'http://localhost:3001'}`
- 后续所有接口调用都走这个封装

### 6. 全局状态 — `src/context/GameContext.jsx`
- 用 `createContext` + `useReducer` 模式
- 初始 state 结构（字段名见 00-总览数据模型）：
  ```javascript
  {
    phase: 'IDLE',           // IDLE | OPEN | ACTIVE | OFF
    nodes: [], edges: [],
    pokes: [], notifications: [],
    currentUser: '小陈',
    integrity: 100, escapeProgress: 0,   // 占位，本任务不用
    metrics: { doneToday: 0, alignedPeople: 0, blocked: 0 }
  }
  ```
- 导出 `GameProvider`、`useGame()`、`PHASES`
- reducer 预留 action：`SET_PHASE`、`SET_NODES`、`SET_EDGES`、`ADD_POKE`、`ADD_NOTIFICATION`、`UPDATE_NODE_STATUS`、`SET_METRICS`、`RESET`

### 7. Vite 代理配置
- `vite.config.js` 里把 `/api` 代理到 `http://localhost:3001`（前后端同机联调）

## 约束
- 不要实现网络图、戳一戳、通知等任何业务逻辑，本任务只搭骨架 + 桌宠入口
- 桌宠**只做最简版**：一个图标 + 点击跳转，不要展开动画、不要拖拽、不要菜单
- 不要安装未提到的依赖包
- `mock-data.json` 本任务保持空占位，不填充数据
- 桌宠点击跳转用 `navigate('/network')`，不要用 `<a href>` 整页刷新（保持 SPA 体验）

## 验证方法
1. 运行 `npm run dev`，浏览器打开 `http://localhost:5173/`，看到右下角桌宠（灯仔），无报错
2. 桌宠在右下角固定位置，不随页面滚动移动
3. 点击桌宠，页面跳转到 `/network`，显示「网络图 TODO」占位文字
4. 运行 `node server/index.js`，终端显示监听 3001
5. `curl http://localhost:3001/api/health` 返回 `{"ok":true}`
6. 在渲染页 `NetworkPage` 里调用 `useGame()`，能读到 `state.phase === 'IDLE'`
7. 控制台无报错
