# 任务 03：协作网络图

## 开发目的
在渲染页 `/network` 实现协作依赖网络图——节点、连线、上下游、并行/分叉/汇聚、瓶颈、关键路径、延期标记，以及节点详情展开。这是产品的「第一眼」，对应分镜 0:07–0:40。

## 前置依赖
- 任务 01（路由 + 全局状态 + API 封装）
- 任务 02（`/api/feishu/*`、`/api/ai/*` 接口可用）

## 需要完成的事项

### 1. 网络图渲染 — `src/components/NetworkGraph.jsx`

**渲染方式：** 用 SVG 或绝对定位 div（不引入 D3 等图形库，保持轻量）。

- 节点用圆形卡片（直径约 56px），显示任务名 + 负责人
- 连线用 `<line>` 或细 div，从上游节点指向下游节点（可加小箭头）
- 节点位置用 mock-data.json 的 `coordinates` 坐标，等比映射到容器

**节点按状态上色：**
| 状态 | 颜色 |
|---|---|
| done（已完成） | 绿 `#22c55e` |
| doing（进行中） | 黄 `#eab308` |
| todo（未开始） | 灰 `#4b5563` |

### 2. 瓶颈卡点标记
- `isBottleneck === true` 的节点（n_design）加**红色呼吸边框**：`opacity` 在 0.6↔1 之间循环（约 1.2s 一轮）
- 节点旁加一个标签 `瓶颈 · 阻塞 2 个下游`

### 3. 关键路径高亮
- `isCritical === true` 的连线（e1~e4）用更亮/更粗的蓝色描边
- 非关键连线（e5、e6）用更暗更细的线，形成视觉对比

### 4. 延期标记 — `DelayImpact` 相关
- `isDelayed === true` 的节点（n_brand）加 `⏰` 标记 + 黄色脉冲边框
- 节点旁标签 `已延期 1 天`

### 5. 节点详情 — `src/components/NodeCard.jsx`
- 悬停（或点击）节点时，节点旁弹出详情卡片
- 详情显示：任务名、负责人、部门、状态、是否延期
- 详情里含「延期连锁影响」预览：若该节点延期，列出受影响的下游任务名（用 edges 推导）

### 6. 网络生长动画（对应分镜 0:07–0:11）
- 进入渲染页时，先调 `POST /api/feishu/auth` → `GET /api/feishu/data` → `POST /api/ai/parse` → `POST /api/ai/approve`
- 拿到 nodes/edges 后，**逐层点亮**：按「关键路径顺序」让节点依次淡入（约每 200ms 一个），连线沿关键路径先亮
- 这个生长动画用来隐喻「AI 解析 → 审批 → 生成网络」（见 SPEC 4.2，压缩呈现，不逐字展示审批）

### 7. 通知条（对应分镜 0:27–0:31）
- 页面顶部一个通知条，首次渲染后显示：`你的「首页设计稿」正在阻塞 2 个下游任务`
- 2 秒后自动淡出

### 8. 数据接入渲染页 — `src/pages/NetworkPage.jsx`
- 用 `GameProvider` 包裹
- 挂载时走第 6 步的飞书链路，把结果 `SET_NODES` / `SET_EDGES` 写入全局状态
- 网络图数据加载中显示「正在同步飞书文档/看板/聊天…」

## 约束
- 不要引入 D3 / vis.js 等图形库，用原生 SVG 或 div
- 网络图不要求可拖拽/缩放（demo 不需要，见 SPEC 7 简化）
- 不要做移动端适配
- 生长动画从 auth 一路走到 approve 才有意义，不要跳过链路直接写死 nodes

## 验证方法
1. 打开 `/network`，先看到「正在同步飞书…」，约 1~2 秒后网络图逐层点亮出现
2. 网络图有 6 个节点，颜色符合状态（2 绿 / 2 黄 / 2 灰）
3. `首页设计稿` 节点有红色呼吸边框 + 「瓶颈」标签
4. `品牌素材` 节点有 `⏰` 标记 + 「已延期 1 天」
5. 关键路径（需求→品牌→设计→开发→测试）连线明显比 `需求→运营文案` 亮
6. 悬停「首页设计稿」弹出详情卡片，含负责人小陈 + 下游影响预览
7. 顶部通知条显示「阻塞 2 个下游」后 2 秒自动消失
8. 控制台无报错
