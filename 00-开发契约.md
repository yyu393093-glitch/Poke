# 「戳戳」开发契约 —— 三人并行必读

> **这份文档是什么**：三个人 + 三个 AI 的共同合同。它是从 [Poke_SPEC.md](../Poke_SPEC.md) 里提炼出来的「必须一致」的部分。
>
> **使用方式**：开工前三人一起读一遍；之后**每个人喂给 AI 时，把这份契约和你的 task 一起喂进去**。AI 只认你给的上下文，不给契约它就会自己发明字段名。
>
> **铁律**：本文里每一个字段名、枚举值、id，都是**硬编码**，改一个字母 = 另外两个人的代码拼不上 = 返工。

---

## 1. 数据模型契约（字段名一个字母都不能错）

### 1.1 节点 Node

```typescript
interface Node {
  id: string;               // 唯一 id，固定值见第 2 节
  name: string;             // 中文任务名，如 '首页设计稿'
  owner: string;            // 负责人，如 '小陈'
  dept: string;             // 部门，如 '设计部'
  status: 'done' | 'doing' | 'todo';   // ⚠️ 枚举小写，不要写成 'Done'/'DONE'/'completed'
  isBottleneck: boolean;    // 是否瓶颈卡点
  isDelayed: boolean;       // 是否延期
  x: number;                // 图上坐标（px）
  y: number;
}
```

### 1.2 连线 Edge

```typescript
interface Edge {
  id: string;
  from: string;        // 上游 Node.id
  to: string;          // 下游 Node.id
  isCritical: boolean; // 是否关键路径
}
```

### 1.3 戳一戳 Poke

```typescript
interface Poke {
  id: string;
  from: string;        // 谁戳（负责人名）
  to: string;          // 戳谁（Node.id）
  message: string;     // 系统自动生成的消息
  channel: 'feishu' | 'wecom' | 'dingtalk';   // 发到哪个端，demo 统一 'feishu'
  time: string;        // '17:57:32'
}
```

### 1.4 通知 Notification

```typescript
interface Notification {
  id: string;
  to: string;          // 发给哪个负责人
  type: 'poke' | 'upstream_done' | 'offline_boundary';
  message: string;
  channel: 'feishu' | 'wecom' | 'dingtalk';
}
```

### 1.5 涟漪 Ripple（前端写死，不走接口）

```typescript
interface Ripple {
  downstreamTasks: string[];   // 下游任务 id 列表
  departments: string[];       // ['研发部','运营部']
  reachUsers: number;          // 1200000
}
```

### 1.6 今日指标 DailyMetrics（走 `/api/metrics`）

```typescript
interface DailyMetrics {
  doneToday: number;      // 3
  alignedPeople: number;  // 5
  blocked: number;        // 0
}
```

---

## 2. 预设数据契约（这些 id 是硬编码，禁止改）

> 理由：连线的 `from/to`、`/api/poke` 的 `to`、`/api/node/complete` 的 `nodeId`、前端写死的「主角节点」全都引用这些 id。改一个 id，全链路断。

### 2.1 六个节点

| id | name | owner | dept | status | isBottleneck | isDelayed | x | y |
|---|---|---|---|---|---|---|---|---|
| `n_req` | 需求文档 | 王姐 | 产品部 | done | false | false | 400 | 80 |
| `n_brand` | 品牌素材 | 陈总 | 设计部 | doing | false | **true** | 200 | 180 |
| `n_design` | 首页设计稿 | 小陈 | 设计部 | doing | **true** | false | 400 | 300 |
| `n_dev` | 前端开发 | 老李 | 研发部 | todo | false | false | 300 | 420 |
| `n_test` | 联调测试 | 小赵 | 研发部 | todo | false | false | 500 | 420 |
| `n_copy` | 运营文案 | 阿May | 运营部 | done | false | false | 600 | 180 |

### 2.2 六条连线

| id | from | to | isCritical |
|---|---|---|---|
| `e1` | n_req | n_brand | true |
| `e2` | n_brand | n_design | true |
| `e3` | n_design | n_dev | true |
| `e4` | n_dev | n_test | true |
| `e5` | n_req | n_copy | false |
| `e6` | n_design | n_test | false |

### 2.3 关键身份（前端会写死这些，不要另起变量）

- **主角（当前用户）**：`小陈`，节点 `n_design`
- **瓶颈卡点**：`n_design`（阻塞 n_dev、n_test 两个下游）
- **延期节点**：`n_brand`
- **关键路径**：`n_req → n_brand → n_design → n_dev → n_test`

---

## 3. API 接口契约（8 个，精确到请求/响应 JSON）

> 前缀 `http://localhost:3001`（前端用 `VITE_API_URL` 或 vite 代理，见 task 01）。所有响应模拟 200~500ms 延迟。

### 3.1 假授权
```
POST /api/feishu/auth
请求：{}
响应：{ "token": "mock-token" }
```

### 3.2 拉取飞书数据
```
GET /api/feishu/data
请求：?token=mock-token
响应：{ "tasks": [ Node, Node, ... ] }   // 6 个节点（不含坐标，坐标前端另取或后端拼）
```

### 3.3 AI 解析（本地规则，不调真 LLM）
```
POST /api/ai/parse
请求：{ "tasks": [...] }
响应：{ "nodes": [...], "edges": [...], "pendingApproval": true }
```

### 3.4 审批通过
```
POST /api/ai/approve
请求：{ "nodes": [...], "edges": [...] }
响应：{ "approved": true, "nodes": [...], "edges": [...] }
```

### 3.5 戳一戳
```
POST /api/poke
请求：{ "from": "小陈", "to": "n_brand" }
响应：{ "message": "陈总好，首页设计稿还差品牌素材，方便今天给我吗？🙏", "channel": "feishu" }
```

### 3.6 标记完成（自动通知下游）
```
POST /api/node/complete
请求：{ "nodeId": "n_design" }
响应：{
  "nodeId": "n_design",
  "notifications": [
    { "id": "ntf1", "to": "老李", "type": "upstream_done", "message": "上游已完成，你可以开始了", "channel": "feishu" },
    { "id": "ntf2", "to": "小赵", "type": "upstream_done", "message": "上游已完成，你可以开始了", "channel": "feishu" }
  ]
}
```

### 3.7 关灯
```
POST /api/clock/off
请求：{}
响应：{ "status": "off" }
```

### 3.8 今日指标
```
GET /api/metrics
响应：{ "doneToday": 3, "alignedPeople": 5, "blocked": 0 }
```

---

## 4. 状态机与枚举契约

```javascript
// 演示阶段（全局 state.phase，注意全大写）
const PHASES = { IDLE: 'IDLE', OPEN: 'OPEN', ACTIVE: 'ACTIVE', OFF: 'OFF' };

// 节点状态（Node.status，注意全小写）
// 'done' | 'doing' | 'todo'
// 流转： todo → doing → done
```

---

## 5. 全局状态契约（GameContext）

### 5.1 state 结构

```javascript
{
  phase: 'IDLE',                 // IDLE | OPEN | ACTIVE | OFF
  nodes: [],                     // Node[]
  edges: [],                     // Edge[]
  pokes: [],                     // Poke[]
  notifications: [],             // Notification[]
  currentUser: '小陈',
  metrics: { doneToday: 0, alignedPeople: 0, blocked: 0 }
}
```

### 5.2 reducer action（共 8 个，名字固定）

| action type | payload | 作用 |
|---|---|---|
| `SET_PHASE` | phase 字符串 | 切演示阶段 |
| `SET_NODES` | Node[] | 覆盖 nodes |
| `SET_EDGES` | Edge[] | 覆盖 edges |
| `ADD_POKE` | Poke | 追加一条戳一戳记录 |
| `ADD_NOTIFICATION` | Notification | 追加一条通知 |
| `UPDATE_NODE_STATUS` | { nodeId, status } | 更新某节点状态 |
| `SET_METRICS` | DailyMetrics | 覆盖指标 |
| `RESET` | 无 | 回到初始状态 |

> ⚠️ 谁要新增 action，先在群里喊，避免和另外两人撞名。

---

## 6. 共享文件所有权（三个会冲突的文件）

| 文件 | 谁定义 | 谁能改 | 改的规矩 |
|---|---|---|---|
| `src/App.jsx` | 阶段 1 的人 | 只有联调时 | 挂新路由/组件先喊 |
| `src/context/GameContext.jsx` | 阶段 1 的人 | 三人 | 只加 action，不改已有字段名 |
| `src/api/gameApi.js` | 阶段 1 的人 | 三人 | 只加函数，不改 `request` 签名 |

> 其余文件各归各的主线（见分工），**别人不碰**。

---

## 7. 可复用组件契约（跨主线的唯一接口）

C（交互）负责写一个灯仔飞行组件，B（网络图）在「通知下游」时也要飞灯仔，所以复用同一个：

```javascript
// C 负责实现并导出，B 直接 import 复用
<FlyingLamp
  from={{ x: number, y: number }}   // 起点坐标
  to={{ x: number, y: number }}     // 终点坐标
  duration={1500}                    // 飞行毫秒，默认 1500
  onArrive={() => {}}                // 抵达回调（可选）
/>
```

> 约定：灯仔 = 暖黄色圆形小角色。C 写好后在群里发一条「FlyingLamp 已就绪，props 见契约 7.1」，B 就能用。

---

## 8. 视觉常量契约

```javascript
// 状态色（全项目统一，不要各写各的十六进制）
const COLOR = {
  done:  '#22c55e',    // 已完成：绿
  doing: '#eab308',    // 进行中：黄
  todo:  '#4b5563',    // 未开始：灰
  bottleneck: '#ef4444',  // 瓶颈：红呼吸边框
  critical: '#3b82f6',    // 关键路径连线：蓝
  delayed: '#f59e0b',     // 延期标记：橙
};
```

---

## 9. 禁令清单（三条主线共同遵守，违反 = 偏离规格）

1. **H1**：不用真实飞书/企微/钉钉的 SDK、API key、URL、品牌 logo（用文字/emoji 占位）。
2. **H3**：不部署任何云，只跑 `localhost`。
3. **不调真实大模型**：AI 解析是写死规则（见 SPEC 4.2）。
4. **不引入额外依赖**：图形用原生 SVG/div，不用 D3/vis.js；路由用 react-router-dom；后端只用 express + cors。
5. **字段名、枚举、id 一律照本契约**，不得"顺手改得更规范"——改了就是返工。

---

*本契约 v1.0 | 上游：[Poke_SPEC.md](../Poke_SPEC.md) · [00-总览与依赖关系.md](./00-总览与依赖关系.md) | 三人开工前各读一遍，喂 AI 时和 task 一起喂*
