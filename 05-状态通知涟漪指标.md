# 任务 05：状态更新、自动通知、涟漪与指标

## 开发目的
实现「完成任务 → 自动通知下游 → 节点点亮」、「延期连锁影响」、个人「涟漪」影响展示，以及「今日对齐度」指标。这是 demo 的情绪高点和价值证明，对应分镜 1:00–1:40。

## 前置依赖
- 任务 02（`/api/node/complete`、`/api/metrics` 接口）
- 任务 03（网络图 + 节点详情）
- 任务 04（戳一戳，已完成，作为上下文）

## 需要完成的事项

### 1. 标记完成 — `StatusUpdate`（并入 NodeCard 或独立按钮）

- 在「首页设计稿」节点详情里放「标记完成」按钮
- 点击 → 调 `POST /api/node/complete { nodeId: 'n_design' }`
- 前端 `UPDATE_NODE_STATUS` 把 n_design 的 status 从 doing → done
- 节点颜色黄 → 绿渐变切换（约 0.5s 过渡动画）

### 2. 自动通知下游 — `NotifyDownstream`

- 拿到 `/api/node/complete` 返回的 `notifications`（n_dev、n_test 两条）
- 两个「灯仔」图标分别飞向「前端开发」「联调测试」节点
- 通知抵达后，这两个节点 status 从 todo → doing，颜色灰 → 黄自动点亮
- 通知内容展示为一条顶部/侧边提示：`上游已完成，你可以开始了`

### 3. 延期连锁影响 — `DelayImpact`（补全任务 03 第 5 步的预览）

- 点击「品牌素材」节点详情时，展示「延期连锁影响」：
  - 若品牌素材延期，下游「首页设计稿」「前端开发」「联调测试」整体顺延 1 天
- 用 edges 从 n_brand 向下推导受影响节点，动态列出，不写死

### 4. 涟漪展示 — `src/components/RippleView.jsx`

- 点击「首页设计稿」节点（或一个「查看影响」按钮），切换到涟漪视图
- 动画：从 n_design 节点向外扩散 3 层同心圆波纹
- 依次点亮受影响链路：前端开发 → 联调测试 → 「首页新版上线」→ 用户
- 影响面板显示三组数据（写死）：
  - 下游任务：2 个
  - 影响部门：研发部、运营部
  - 触达用户：120 万

### 5. 今日对齐度指标 — `src/components/MetricsBar.jsx`

- 调 `GET /api/metrics` 拿 `{ doneToday, alignedPeople, blocked }`
- 渲染页顶部展示：`今日完成 3 项 · 对齐 5 人 · 阻塞 0`
- 数字用强调色，出现时带淡入动画

## 约束
- 状态更新、通知、涟漪全部走接口拿数据，不写死在前端（涟漪的三组数据除外，可写死）
- 灯仔飞行动画复用任务 04 的实现，不要重复写一套
- 不要做真实的多人实时同步，状态变化是本地单机的

## 验证方法
1. 点击「首页设计稿」→「标记完成」，节点由黄变绿，约 0.5s 过渡
2. 两个灯仔分别飞向「前端开发」「联调测试」，两节点由灰变黄点亮
3. 顶部出现「上游已完成，你可以开始了」通知
4. 点击「品牌素材」详情，看到 3 个受影响下游任务（首页设计稿/前端开发/联调测试）
5. 点击「首页设计稿」→「查看影响」，看到波纹扩散 + 影响面板显示「120 万用户 / 研发部+运营部 / 2 个下游」
6. 页面顶部指标显示「今日完成 3 项 · 对齐 5 人 · 阻塞 0」
7. 控制台无报错
